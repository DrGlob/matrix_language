let a = 1;
for(i in 1..5) {
    a = a + 1;
}

if (a == 5) {
    print(a);
    let b = 10;
}

let add = function(a, b) { return a + b; };
print(add(2, 3));
let x = add(7,9);
print(x);

let double = function(x) { return x * 2; };

let matrix = [[1, 2], [3, 4]];
let doubledMatrix = map(matrix, double);
print(doubledMatrix);